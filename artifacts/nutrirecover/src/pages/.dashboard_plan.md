 # Recovery Dashboard Plan

## Information Gathered
- The existing `Dashboard.tsx` at `old-project/artifacts/nutrirecover/src/pages/Dashboard.tsx` already implements ALL 6 requested sections:
  1. **Recovery Score** - via `data.recoveryScore` with Progress bar
  2. **Deficiencies** - via `deficiencies` filtered from `activeNutrients` 
  3. **Meals Completed** - via `mealsCompleted` derived from `dailySummary.mealLogs`
  4. **Today's Nutrient Intake** - via `todayNutrientData` with nutrient progress bars
  5. **Weekly Progress** - via `weeklyProgress` with Recharts BarChart
  6. **Expected Recovery Time** - via `expectedRecoveryDays` + `recoveryProgress`
  
- The Dashboard is already routed via `App.tsx` at path `/dashboard`
- Data comes from: `useGetDashboard`, `useGetProfile`, `useGetDailySummary`, `useGetSuggestions` hooks
- Backend route `GET /profiles/:profileId/dashboard` in `dashboard.ts` provides the core data
- The Layout component wraps the Dashboard and provides sidebar navigation

## Plan
- The existing Dashboard.tsx is complete and already has all 6 sections requested.
- The key improvements needed from `patch_plan.md` and `task_progress.md`:
  1. Ensure the Dashboard renders all sections cleanly
  2. Add "Edit Assessment" navigation button (to /assessment) in the Quick Actions section
  3. Handle loading, empty, and error states gracefully

## Edits to Dashboard.tsx
1. Add an "Edit Assessment" button in the Quick Actions section alongside existing buttons
2. Ensure all 6 requested display sections are clearly labeled and prominent
3. Keep the existing design intact

## Follow-up Steps
- Verify TypeScript compiles after changes
- The Dashboard should be accessible from Home page's "Continue to Dashboard" button

